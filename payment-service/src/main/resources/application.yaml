spring:
    application:
        name: payment-service
    datasource:
        url: jdbc:postgresql://localhost:5432/payment_service
        username: ganesh
        password: ganesh


    jpa:
        hibernate:
            ddl-auto: update   # options: none, update, create, create-drop
        show-sql: true       # prints SQL queries in console
        properties:
            hibernate:
                format_sql: true
        database-platform: org.hibernate.dialect.PostgreSQLDialect
    servlet:
        multipart:
            enabled: true
            max-file-size: 10MB
            max-request-size: 10MB    
server:
        port: 8083
security:
  basic:
    enabled: false
management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


razorpay:
   key: "rzp_test_Sndc7l6CXKpE25"
   secret: "cT3wu2s0MdbtThRko29SEtP0"